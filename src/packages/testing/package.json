{
  "name": "@lightning-jackpot/testing",
  "main": "dist/index.js",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn run clean && tsc",
    "dev": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "typeorm": "^0.2.0"
  },
  "devDependencies": {
    "debug": "^4.0.0",
    "rimraf": "^3.0.0",
    "typescript": "^3.7.0"
  }
}
