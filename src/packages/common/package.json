{
  "name": "@lightning-jackpot/common",
  "main": "dist/index.js",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn run clean && tsc",
    "dev": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "provably-fair-framework": "*"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "typescript": "^3.6.0"
  }
}
